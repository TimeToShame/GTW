cat > render.yaml << EOF
services:
  - type: web
    name: gift-bot-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port \$PORT
    envVars:
      - key: BOT_TOKEN
        sync: false
EOF
